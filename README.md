# opened questions
Active settings in current lsp client configuration differs than settings in enabled client configuration, why? what are real settings current client work with?

inc-rename rename fields don't works until don't try to rename class
    question still remains: why it works properly after running setup from <lspconfig> implicitly?
